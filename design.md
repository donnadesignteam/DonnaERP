# Design — Donna ERP

ระบบดีไซน์กลางของเว็บนี้ ทุกหน้าที่จะรีดีไซน์ต้องอ่านไฟล์นี้ก่อนเขียนโค้ด
ห้ามเลือกธีมใหม่รายหน้า (แอปหลายบุคลิก = slop) ถ้าระบบต้องโตให้แก้ไฟล์นี้

## Genre
editorial — กระดาษ/สมุดงาน เส้นคั่นบางแทนกรอบการ์ด ตัวเลขเป็นพระเอก

## Macrostructure family
- หน้าแดชบอร์ด/จอติดผนัง: **19 Map / Diagram** (แผนผังสายการผลิตเป็นตัวจัดหน้า) + ตารางบัญชีด้านล่าง
- หน้าทำงาน (ออเดอร์/เคลม/ปฏิทิน): **Workbench** — ตารางหนาแน่นคือเนื้อหาหลัก แถบตัวกรองบนสุด
- หน้าอ่าน/ตั้งค่า: Long Document

## Theme (ใช้ตัวแปรใน app/globals.css เท่านั้น ห้ามฮาร์ดโค้ดสีกลางไฟล์)
- `--bg` #FAF8F5 กระดาษครีม · `--surface` #FFFFFF
- `--ink` #1A1008 · `--ink-2` #3D2E1E · `--ink-3` #7A6A58 · `--ink-4` #A89880
- `--border` #EAE5DF (เส้นคั่น hairline) · `--border-2` #D9D2C8
- `--blue` #C47E3A = สีเน้นคาราเมล **ใช้ไม่เกิน 5% ของหน้าจอ**
- `--red` #DC2626 = เตือนเท่านั้น (เกินกำหนด/ยกเลิก) ห้ามใช้ตกแต่ง
- สีขั้นผลิตใช้ `STATUS_COLOR` ชุดเดียวทั้งเว็บ (lib/orderTabs.ts) ห้ามตั้งสีแผนกเองซ้ำ

## Typography
- Display (ตัวเลขใหญ่/หัวหน้า): `--font-display` = Georgia, 'Times New Roman', serif — มีในทุกเครื่อง ไม่ต้องโหลด
- Body: Geist / system sans เดิม
- ตัวเลขทุกที่ `font-variant-numeric: tabular-nums`
- ห้ามหัวข้อ 11px uppercase letter-spacing (eyebrow) — หัวข้อคือ 15-16px น้ำหนัก 600 ตัวปกติ

## Spacing
สเกล 4/8/12/16/24/32/48 · มุมโค้งค่าเดียว 10px (ป้ายเล็ก 6px) · เงาค่าเดียว `--shadow`

## Motion
เงียบ — เปลี่ยนสีพื้น/เส้นขอบเท่านั้น 120ms
**ห้าม** ยกลอยตอนชี้ (translateY) · ห้ามเงาบาน · ห้าม transition-all

## จอติดผนัง (โหมดเต็มจอของหน้าภาพรวม)
- อ่านจากระยะ 2-3 เมตร: ตัวเลขหลัก ≥ 44px, ป้ายกำกับ ≥ 15px, ห้ามตัวอักษรต่ำกว่า 13px ในครึ่งบนของหน้า
- ห้ามซ่อนข้อมูลไว้ใต้การชี้เมาส์/ป๊อปอัป — จอติดผนังไม่มีใครถือเมาส์
- ข้อมูลสำคัญต้องอยู่ครบในหน้าจอแรกโดยไม่ต้องเลื่อน

## หน้าแอปห้ามมี
การ์ดพื้นไล่สี · ไอคอนในสี่เหลี่ยมมนสีอ่อน · การ์ดซ้อนการ์ด · เงาสี · การ์ดขนาดเท่ากันเรียง 3 ใบ ·
ภาพประกอบ/ของตกแต่งทุกชนิด (ฟังก์ชันเป็นตัวพาหน้า)

## ทุกหน้าต้องใช้ร่วมกัน
สีเน้นคาราเมลและตำแหน่งที่ใช้ · ชุดเส้นคั่น hairline · จังหวะหัวข้อ · ปุ่ม (ขอบบาง พื้นขาว มุม 10px)
